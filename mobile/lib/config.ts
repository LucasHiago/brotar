/**
 * URL do backend do Brotar. Ainda não definido (Supabase x próprio) — ver
 * docs/open-questions.md. Por enquanto o app roda com dados locais (lib/mock.ts).
 */
export const BACKEND_URL = 'https://api.brotar.local';

/** Liga/desliga o uso de dados de exemplo enquanto não há backend. */
export const USE_MOCK = true;
